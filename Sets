-- Function to kick the player with a custom message
local function kickPlayer()
    local player = game.Players.LocalPlayer
    
    if player then
        -- Customize the message here
        local message = "ur not whitelisted to use this"
        
        -- Kick the player with the custom message
        player:Kick(message)
    end
end

kickPlayer()
